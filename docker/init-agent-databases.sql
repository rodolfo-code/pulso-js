-- Databases para agentes LangGraph (AsyncPostgresSaver)
-- Adicionar novos databases aqui quando novos agentes forem criados

CREATE EXTENSION IF NOT EXISTS "uuid-ossp";

-- agent_standard já criado via POSTGRES_DB
-- Futuro: CREATE DATABASE agent_copilot_retail;
